move_speed     = 4.8;     // 可再調
target_x       = x;
target_y       = y;
target_obj     = noone;
carry_coffee   = false;
arrival_radius = 10;

// 由 o_game 指定
min_x_bound = 0;
max_x_bound = room_width;
side        = 0; // 1=P1, 2=P2（純標記）

depth = -10; // 比收銀台更前
